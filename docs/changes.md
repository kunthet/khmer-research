## Feature: XML Sitemap
**Purpose:** 
Expose a standard `sitemap.xml` so search engines can better discover and crawl KhmerResearch.com.

**Implementation:** 
Added `sitemap.xml` at the site root with the main homepage URL and wired it into `robots.txt` via a `Sitemap` directive.

**History:**
- Created — Initial sitemap for primary homepage URL and robots.txt integration.

